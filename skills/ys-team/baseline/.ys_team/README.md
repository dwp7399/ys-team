# baseline local ys-team

这是默认工作流骨架的示例 `.ys_team/`。

`ys-team-init` 应以这里的结构为来源，再结合具体项目现实生成项目自己的 `.ys_team/`。

建议读取顺序：

1. `TEAM.md`
2. `.ys_team/methods.md`
3. `.ys_team/policy.md`
4. `.ys_team/delivery-flow.md`
5. `.ys_team/templates/`
