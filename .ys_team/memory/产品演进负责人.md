# 产品演进负责人 经验记忆

## full-auto 验证载体选择（2026-04-15）

**错误模式**：选过大或过抽象的载体验证 full-auto 链路，执行周期长、验收证据难以明确、中途容易被打断。

**正确做法**：选"小而可见"的改动作为载体——能同时覆盖用户可见输出、文档同步和验收证据的最小改动。例如给 skill 加一条通知规则，比重构一个模块更适合作为验证载体。

**适用场景**：需要验证编排机制（full-auto/semi-auto）是否可用时；选测试载体时优先考虑"覆盖面"而不是"改动量"。
